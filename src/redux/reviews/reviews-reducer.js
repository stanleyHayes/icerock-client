const INITIAL_STATE = {
    reviews: [],
    error: null,
    loading: false
}

const reviewsReducer = (state = INITIAL_STATE, action) => {

    switch (action.type){

        default: return state;
    }

}


export default reviewsReducer;
